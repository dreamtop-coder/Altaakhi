# Example .env for local development
# Copy to .env and fill real values (DO NOT commit real .env to VCS)

# Secret key for Django
DJANGO_SECRET_KEY=replace-this-with-a-strong-secret

# Debug mode for local dev (True/False)
DJANGO_DEBUG=True
